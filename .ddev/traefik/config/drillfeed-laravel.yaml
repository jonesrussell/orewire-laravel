#ddev-generated
# If you remove the ddev-generated line above you
# are responsible for maintaining this file. DDEV will not then
# update it, for example if you add `additional_hostnames`, etc.

http:
  routers:
    drillfeed-laravel-web-80-http:
      entrypoints:
        - http-80
      rule: HostRegexp(`^drillfeed-laravel\.ddev\.site$`)
      
      service: "drillfeed-laravel-web-80"
      ruleSyntax: v3
      tls: false
      # middlewares:
      #   - "drillfeed-laravel-redirectHttps"
    drillfeed-laravel-web-8025-http:
      entrypoints:
        - http-8025
      rule: HostRegexp(`^drillfeed-laravel\.ddev\.site$`)
      
      service: "drillfeed-laravel-web-8025"
      ruleSyntax: v3
      tls: false
      # middlewares:
      #   - "drillfeed-laravel-redirectHttps"
    drillfeed-laravel-xhgui-80-http:
      entrypoints:
        - http-8143
      rule: HostRegexp(`^drillfeed-laravel\.ddev\.site$`)
      
      service: "drillfeed-laravel-xhgui-80"
      ruleSyntax: v3
      tls: false
      # middlewares:
      #   - "drillfeed-laravel-redirectHttps"
    
    
    
    drillfeed-laravel-web-80-https:
      entrypoints:
        - http-443
      rule: HostRegexp(`^drillfeed-laravel\.ddev\.site$`)
      
      service: "drillfeed-laravel-web-80"
      ruleSyntax: v3
      
      tls: true
      
    drillfeed-laravel-web-8025-https:
      entrypoints:
        - http-8026
      rule: HostRegexp(`^drillfeed-laravel\.ddev\.site$`)
      
      service: "drillfeed-laravel-web-8025"
      ruleSyntax: v3
      
      tls: true
      
    
    drillfeed-laravel-xhgui-80-https:
      entrypoints:
        - http-8142
      rule: HostRegexp(`^drillfeed-laravel\.ddev\.site$`)
      
      service: "drillfeed-laravel-xhgui-80"
      ruleSyntax: v3
      
      tls: true
      
    

  middlewares:
    drillfeed-laravel-redirectHttps:
      redirectScheme:
        scheme: https
        permanent: true

  services:
    drillfeed-laravel-web-80:
      loadbalancer:
        servers:
          - url: http://ddev-drillfeed-laravel-web:80
        
    drillfeed-laravel-web-8025:
      loadbalancer:
        servers:
          - url: http://ddev-drillfeed-laravel-web:8025
        
    
    
    drillfeed-laravel-xhgui-80:
      loadbalancer:
        servers:
          - url: http://ddev-drillfeed-laravel-xhgui:80
        
    
    

tls:
  certificates:
    - certFile: /mnt/ddev-global-cache/traefik/certs/drillfeed-laravel.crt
      keyFile: /mnt/ddev-global-cache/traefik/certs/drillfeed-laravel.key